{
  "name": "chapter_4",
  "version": "1.0.0",
  "description": "A dockeries full stack backend application that uses a NodeJS backend, a prismera ORM and JWT Authentication",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --trace-warnings --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.15.0"
  }
}
